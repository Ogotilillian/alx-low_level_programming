#include <stdio.h>

/**
 * main - entry point
 * @argc: stands for argument count
 * @argv: stands for argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
